# 依赖库（绝对不能上传，因为 Vercel 会自己下载）
node_modules/

# 本地打包结果（Vercel 会在云端重新打包）
dist/
dist-ssr/
*.local

# 日志文件
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 环境变量（如果以后有私密 Key，防止泄露）
.env
.env.local

# 编辑器配置
.vscode/
.DS_Store